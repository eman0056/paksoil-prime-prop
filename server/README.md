# PakSoils Contact API

## Setup

1. Copy `.env.example` to `.env`.
2. Fill in the SMTP credentials for your email provider.
3. Start the server with:

```bash
node server/index.js
```

## Endpoints

- `GET /health`
- `POST /api/contact`
